name: Build ginkgo kernel 4.14.117

on:
  workflow_dispatch:
  push:
    branches: [ main ]

env:
  KERNEL_REPO: https://github.com/MiCode/Xiaomi_Kernel_OpenSource
  KERNEL_BRANCH: ginkgo-q-oss
  DEFCONFIG: vendor/ginkgo-perf_defconfig

jobs:
  build:
    # ubuntu-22.04: older glibc (no DT_RELR/.relr.dyn) so proton's LLD can link host tools
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout packaging repo
        uses: actions/checkout@v4

      - name: Install build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison flex libssl-dev make gcc \
            git zip curl python3 libncurses-dev

      - name: Clone kernel source (4.14.117 base)
        run: git clone --depth=1 -b "$KERNEL_BRANCH" "$KERNEL_REPO" kernel

      - name: Clone proton-clang (CC)
        run: git clone --depth=1 https://github.com/kdrag0n/proton-clang clang

      - name: "Clone AOSP GCC 4.9 (GNU binutils - aarch64 as/ld/elfedit)"
        # stock Makefile forces -no-integrated-as; clang needs a GNU toolchain whose
        # `as` is aarch64 (not host x86). AOSP GCC 4.9 is the era-matched combo.
        run: |
          git clone --depth=1 \
            https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 gcc64
          git clone --depth=1 \
            https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9 gcc32
          echo "aarch64 as: $(gcc64/bin/aarch64-linux-android-as --version | head -1)"

      - name: Disable CONFIG_MODULE_SIG_FORCE
        working-directory: kernel
        run: |
          cfg="arch/arm64/configs/$DEFCONFIG"
          sed -i 's/^CONFIG_MODULE_SIG_FORCE=y/# CONFIG_MODULE_SIG_FORCE is not set/' "$cfg"
          echo "----- module sig config after patch -----"
          grep -E "CONFIG_MODULES|CONFIG_MODULE_SIG" "$cfg" || true

      - name: Build kernel
        working-directory: kernel
        run: |
          # empty .scmversion suppresses the trailing "+" from setlocalversion,
          # so vermagic stays "4.14.117-perf" and matches stock vendor modules
          : > .scmversion
          export PATH="$GITHUB_WORKSPACE/clang/bin:$PATH"
          export ARCH=arm64 SUBARCH=arm64
          GCC64="$GITHUB_WORKSPACE/gcc64/bin/aarch64-linux-android-"
          GCC32="$GITHUB_WORKSPACE/gcc32/bin/arm-linux-androideabi-"
          # host GCC 11+ defaults to -fno-common; 4.14's bundled dtc has duplicate
          # tentative symbols (yylloc). -fcommon restores merge behavior for host tools.
          HOSTCFLAGS="-Wall -Wmissing-prototypes -Wstrict-prototypes -O2 -fomit-frame-pointer -std=gnu89 -fcommon"
          make O=out ARCH=arm64 "$DEFCONFIG"
          # compile: clang + AOSP gcc assembler + llvm binutils (proven to compile all .o)
          # link: GNU bfd ld (from proton) instead of ld.lld — lld rejects R_AARCH64_ABS32
          #       relocs (EFI stub, MODVERSIONS __crc_*) in this 4.14 tree.
          make -j"$(nproc)" O=out ARCH=arm64 \
            CC=clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE="$GCC64" \
            CROSS_COMPILE_ARM32="$GCC32" \
            LD=aarch64-linux-gnu-ld.bfd \
            AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip \
            HOSTCFLAGS="$HOSTCFLAGS" \
            Image.gz-dtb

      - name: Package flashable zip (AnyKernel3)
        run: |
          IMG=kernel/out/arch/arm64/boot/Image.gz-dtb
          test -f "$IMG" || { echo "Image.gz-dtb not built"; exit 1; }
          cp "$IMG" anykernel/Image.gz-dtb
          cd anykernel
          zip -r9 "../ginkgo-custom-4.14.117-$(date +%Y%m%d).zip" . -x ".git*"

      - name: Upload flashable zip
        uses: actions/upload-artifact@v4
        with:
          name: ginkgo-custom-kernel-zip
          path: ginkgo-custom-4.14.117-*.zip
